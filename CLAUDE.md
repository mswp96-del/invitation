# CLAUDE.md

이 파일은 이 저장소에서 작업할 때 Claude Code에 대한 가이드입니다.

## 프로젝트 개요

스마트폰에서 보기 좋은 **모바일 반응형 초대장** 웹페이지입니다.
**환갑잔치 식사 모임(2026년 10월 26일)** 을 안내하고, 남은 시간을 보여주는
D-Day 카운트다운과 장소 지도를 제공하는 단일 페이지 웹앱입니다.

## 기술 스택

- **프레임워크/빌드 도구 없음** — 순수 HTML, CSS, JavaScript(바닐라)만 사용
- 외부 라이브러리 최소화 (지도 SDK 정도만 예외)
- 별도의 빌드·설치 단계 없이 브라우저에서 바로 열림

## 실행 방법

빌드가 필요 없습니다. `index.html`을 브라우저에서 직접 열면 됩니다.

로컬 서버로 확인하려면(지도 API 등 파일 프로토콜 이슈 방지):

```bash
python -m http.server 8000
```

그 후 브라우저에서 `http://localhost:8000` 접속.

## 파일 구조 (권장)

```
모바일 초대장/
├── index.html      # 초대장 마크업
├── style.css       # 스타일 (모바일 우선)
├── script.js       # D-Day 카운트다운, 지도 초기화 등
└── assets/         # 이미지·아이콘 등
```

## 핵심 기능

### 1. 모바일 반응형 레이아웃
- **모바일 우선(mobile-first)** 으로 작성하고 미디어 쿼리로 넓은 화면을 보정한다.
- 콘텐츠 폭은 세로형 화면 기준(예: `max-width: 480px`)으로 가운데 정렬한다.
- `<meta name="viewport" content="width=device-width, initial-scale=1">` 필수.
- 터치 대상(버튼·링크)은 최소 44×44px 이상 확보.
- 이미지는 `max-width: 100%`로 넘치지 않게 한다.

### 2. D-Day 카운트다운
- 행사 일시를 상수(예: `const EVENT_DATE = new Date('2026-09-20T14:00:00')`)로 관리한다.
- `setInterval`로 매초 남은 일·시·분·초를 갱신한다.
- 행사가 지나면 "행사가 종료되었습니다" 같은 안내로 대체한다.
- 사용자 기기의 로컬 시간대 차이를 고려한다(필요 시 KST 기준 명시).

### 3. 장소 지도
- **카카오맵** JavaScript SDK로 지도를 표시한다.
- API 키(JavaScript 키)는 `index.html`의 SDK 스크립트 태그에 넣되,
  카카오 개발자 콘솔에서 사용 도메인 제한을 걸어 노출 위험을 줄인다.
- 지도 위에 행사 장소 마커를 찍고, 주소 텍스트와 "길찾기/지도앱에서 열기" 링크를 함께 둔다.
- SDK 로드 실패에 대비해 정적 지도 이미지나 주소 텍스트를 대체 표시한다.

## 작업 규칙

- 한글 콘텐츠가 많으므로 파일은 **UTF-8**로 저장한다.
- 실제 개인정보(이름·전화번호·주소·API 키)는 하드코딩된 예시값임을 인지하고,
  변경 요청이 없으면 임의로 바꾸지 않는다.
- 새 기능은 별도 라이브러리 도입 없이 바닐라 JS로 우선 구현한다.
- 스타일 값(색상·폰트·간격)은 CSS 커스텀 속성(`:root` 변수)으로 관리해 테마 변경을 쉽게 한다.
- 모든 변경은 실제 모바일 뷰(폭 375px 전후)에서 깨지지 않는지 확인한다.
